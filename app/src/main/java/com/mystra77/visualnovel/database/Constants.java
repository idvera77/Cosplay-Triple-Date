package com.mystra77.visualnovel.database;

public class Constants {
    private static final String DATABASE_NAME = "Mystra77VisualNovel";
    private static final int DATABASE_VERSION = 1;

    private static final String TABLE_GAME = "game";

    private static final String LEVEL = "level";
    private static final String TSUNDERE = "tsundere";
    private static final String NEKO = "neko";
    private static final String MATURE = "mature";
    private static final String TIME = "time";
    private static final String SCORE = "score";
    private static final String KEY_ID = "id";
    //Timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
    // Tabla Jugador
    /*
    private static final String CREATE_TABLE_JUGADOR = "CREATE TABLE " + TABLE_GAME + "("
            + KEY_ID + " INTEGER PRIMARY KEY NOT NULL, " + NOMBRE + " VARCHAR(30)," + PASSWORD + " VARCHAR(30)," +
            VIDA + " INTEGER," + MANA + " INTEGER," + ATAQUE + " INTEGER," + DEFENSA + " INTEGER," + PUNTUACION + " INTEGER);";
    */
}
